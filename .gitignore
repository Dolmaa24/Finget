# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Environment (never commit real secrets)
.env
.env.local
finget-backend/.env

# Agent worktree scaffolding — created by tooling, never project content.
# `**/` because these appear beside any package (finget-backend/.claude/...),
# and a pattern containing a mid-path slash is anchored to this file's directory.
**/.claude/worktrees/
